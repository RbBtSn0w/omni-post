/**
 * Type definitions for OmniPost Node.js Backend.
 *
 * All shared types are now sourced from @omni-post/shared (SSOT).
 * This file re-exports them for backwards compatibility.
 */

// Re-export all shared types
export { PlatformType } from '@omni-post/shared';
export type { UserInfo, Task, UploadOptions } from '@omni-post/shared';
